# kendeda_test_proj

*`sensor_simulator.py`* - set up to generate raw sensor telemetry data from given examples from the real-life Kendeda building

*`sensor_normalizer.py`* - set up to take in raw telemetry and shorten it into normalized *telemetry classes* defined later in the sys
